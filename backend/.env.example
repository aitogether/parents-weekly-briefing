# 开发环境配置
WECHAT_APPID=
WECHAT_APPSECRET=***

# 数据加密密钥（AES-256，64位十六进制字符串）
# 使用 `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` 生成
DATA_ENCRYPTION_KEY=change_me_to_64_hex_chars_generated_by_crypto_randomBytes_32

NODE_ENV=development
